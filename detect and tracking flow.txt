detect and tracking flow

Init
1. new arController
2. arController add trackable config/pattern files

Main Loop
3. arController->capture get frames
aw4. arController->update => squareTracker->update
    arDetectMarker

        every 8 frames will detect with enhanced threshold adjustmennt
        以 thresholdBracketOver 和 thresholdBracketUnder 來調整 threshhold並與原始threshold作比較
        只要其中之一通過就算過，並將結果較好的那個threshold設為基準threshold以作後續使用

        發想：可以改成在sub thread 做
        
        


        5. Labeling     1.3~2.6 ms
            label feature area
            label->clip 0   start x index
                        1   end x index
                        2   start y index
                        3   end y index
        6. Detect Marker 2 => get contour => check square     0.1~0.2 ms

            Get Contour:
                8 directions search contour
                start from (sx, sy) and one direction ex: (1,0)
                if next point intensity > 0 then add it into contour list
                keep going until return to origin (sx, sy)

            Check square:

                start from orgin find the farest point in the contour list v1
                then find the intermediate point wv1 , wv2

                

        7. GetMarker Info => pattern_match      0.4~0.8 ms      2Target 0.2~1.6 ms

            arPattGetImage2 : get image data

                get_cpara : use 2D world and local position to calculate para(transform matrix) 
                
                use para to transform per pixel from world to local to get image data

            
            pattern match:

                search the whole search space
                calculate correlation operation



        8. Calculate confidence value. 
            If history mode is enabled, compare the value with history record.
            If histoy value is higher, use the record instead.

            confidence value:
                confidence value is generated in genMarkerInfo



    updateWithDetectedMarkers(Multi)        
        9. Get transform matrix / calculate error
        10. If error check passed,  update marker tracker transform matrix.


future:
    1. 顯示最大區域的方塊，後續只追蹤此方塊，直到lost
    2. combine error lost value
    3. .mc file (將.dat和patt包成單一binary file)
    4. 1:2:1 的版本，實體檔案跟測試影片
